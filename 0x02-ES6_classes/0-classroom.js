export default class ClassRoom {
  // Constructor method using maxStudentsSize as an argument
  constructor(maxStudentsSize) {
    // Assign the value of maxStudentsSize to the private property _maxStudentsSize
    this._maxStudentsSize = maxStudentsSize;
  }
}
